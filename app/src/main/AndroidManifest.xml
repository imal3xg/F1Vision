<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/f1visionlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/f1visionlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.F1Vision"
        tools:targetApi="31">
        <!-- Define la actividad SplashScreenActivity como la actividad de inicio -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Puedes definir cualquier configuración adicional para MainActivity aquí -->
        </activity>
    </application>
</manifest>